export default {
  beats: [
    { side: 'left', html: `<span class="eyebrow">第 05 章 · 这台机器</span>
      <h2>Transformer</h2>
      <p class="lead">这就是那张开启了这一切的图——出自 2017 年那篇论文 "Attention Is All You Need" 的 <strong>Transformer 区块</strong>(block),它是每一个现代大语言模型内部的引擎。它看上去密密麻麻,所以让我们把它打开,看一句话如何一个方框接一个方框地从中向上攀爬。</p>
      <p>从下往上读。词元从底部进入,沿着中央的一条<strong>残差流</strong>(residual stream)向上流过寥寥几个方框,而对下一个词的预测便从顶端落出。整个区块交回的那一排向量,形状与它收到时分毫不差——这正是能把它堆得很深的秘诀。</p>` },
    { side: 'right', html: `<h3>输入——先嵌入,再定位</h3>
      <p>首先,每个词元都会变成一个<strong>向量</strong>(也就是上一章里的那种嵌入)。但下一个方框——注意力——对顺序视而不见:把这一排打乱,它交回的还是同样那些向量;在它看来,<span class="tok">The cat sat</span> 和 <span class="tok">sat cat The</span> 一模一样。</p>
      <p>于是,在向上攀爬开始之前,每个词元都会被<em>加</em>上一个<strong>位置编码</strong>(positional encoding)——一个仅由槽位(第一、第二、第三…)定下的向量。如今,"那个词"也一并带上了"那个词坐落在何处"。这件事只发生一次,就在最底部。</p>` },
    { side: 'left', html: `<h3>掩码多头注意力</h3>
      <p>第一个方框,正是词元彼此<strong>交谈</strong>的地方。每个词元都形成一个查询,横向看遍整排,从对它要紧的词那里汲取含义——正是注意力那一章里的那套机制,只不过由好几个<strong>头</strong>并行运作,每个头都在留意一种不同的关系。</p>
      <p>"<strong>掩码</strong>"正是大语言模型特有的转折:一个词元只能关注它<em>前面</em>的词,绝不看它后面的——看那些连线只朝一个方向跑。模型正在学着预测下一个词,所以让它偷看答案就成了作弊。</p>
      <p class="aside">这是信息在词元<em>之间</em>流动的唯一一个方框。区块里其余的一切,都把每个位置各自单独处理。</p>` },
    { side: 'right', html: `<h3>残差 + 归一化(Add &amp; Norm)——那条捷径</h3>
      <p>注意那条<em>绕过</em>注意力方框的箭头。区块从不替换掉一个词元——而是把注意力的结果<strong>加</strong>回到原来的输入上。这条旁路就是<strong>残差连接</strong>(residual connection),正是它让信号(以及训练时的梯度)能在极深的堆叠中存活下来。</p>
      <p>随后,<strong>LayerNorm</strong> 会为每个词元的向量单独重新缩放,让这些数字保持健康,而不至于随着塔越长越高而爆炸或消失。先相加,再归一化——你在下一个方框之后,会再次看到这一模一样的组合。</p>` },
    { side: 'left', html: `<h3>前馈——每个词元独自思考</h3>
      <p>如今,每个词元都<em>独自</em>得到精炼,不再横向张望。<strong>前馈</strong>方框是一个小小的<strong>两层神经网络</strong>(一个 MLP),作用在每个位置上:它把向量撑大到约 <strong>4× 宽</strong>,让它弯折着穿过一个非线性,再把它投影回原来的尺寸——看它扇形展开,又坍缩回来。</p>
      <p>没有任何查表;每一个权重都是学来的。如果说注意力是词元彼此<em>共享</em>的地方,那么前馈就是每个词元<em>消化</em>自己所听到的东西的地方。它的输出会得到同样的<strong>残差 + 归一化</strong>处理——先走残差捷径,再做归一化。</p>` },
    { side: 'right', html: `<h3>一个区块——现在把它堆叠 × N</h3>
      <p>注意力、残差 + 归一化、前馈、残差 + 归一化:这就是<em>一个</em>区块。一个真正的 Transformer 会堆叠<strong>几十个</strong>这样的区块(就是那个著名的 "N×"),它们形状一致,却各自带着<em>自己</em>学来的权重,因此每一层都可以自由地去做一件不同的工作。</p>
      <p>同一排向量沿着那条残差流,笔直地穿过所有这些区块向上流动——每个区块读取它,加上自己微小的修改,再把它传递下去。</p>` },
    { side: 'left', html: `<h3>含义随深度逐渐清晰</h3>
      <p>看那精炼如涟漪般向上荡开。靠前的区块捕捉简单的东西——语法、词的边界、谁挨着谁。更深的区块则朝着抽象含义、意图,以及句子中跨越远距离的关联去构建。</p>
      <p>每个区块都把向量轻轻推动一点;深深堆叠起来,这些推动便累积成对文本一种丰富的、感知语境的解读。</p>
      <p class="aside">这不只是一个比喻——去探查层与层之间的那条流,这种递进就会显现出来:较低的层追踪词性,较高的层承载含义与指代。每一层都把一些特征留给下一层取用。</p>` },
    { side: 'right', html: `<h3>线性 → Softmax → 下一个词元</h3>
      <p>在最顶端,模型取出<em>最后</em>一个位置的那个最终向量,把它送过<strong>线性</strong>方框——一个巨大的<strong>输出矩阵</strong>,词汇表里每个词对应一行——从而为每一个可能的下一个词元,把它变成一个原始分数,一个 <strong>logit</strong>。</p>
      <p>随后 <strong>Softmax</strong> 把那一排参差不齐的分数压成干净利落、加起来等于一的概率。对于 <span class="tok">The cat sat on the…</span>,有一根柱条远远高过其余:<span class="tok">mat</span>。</p>
      <p class="aside">那个输出矩阵就是<strong>反嵌入</strong>(unembedding)——它是底部那张嵌入表的一面镜子。一个把词 → 向量;另一个把最终向量 → 一场关于词的投票。</p>` },
    { side: 'left', html: `<h3>整台机器</h3>
      <div class="postcard">先把词元嵌入并安上位置,让它们共享含义(掩码注意力),再让每个词元独自精炼(前馈),并在每一步之后保留一条残差捷径和一次归一化。把那个区块堆叠 × N,读出顶端的向量,再用线性 + Softmax 在整张词汇表上为它打分——下一个词便落了出来。</div>
      <p class="aside">这就是<strong>仅解码器 Transformer</strong>(decoder-only)的全部,也就是 GPT 这类模型背后的那个形态。从这里往后,不再有新的思想在等着你——接下来的,仍是这同一台机器,只是喂进了多得多的文本,并放大到大得多的规模。</p>
      <div class="deepdive-row">
        <a class="deepdive" data-route="/deep/attention-math">注意力的数学</a>
        <a class="deepdive" data-route="/pretraining">下一章:预训练 →</a>
      </div>` },
  ],
}
