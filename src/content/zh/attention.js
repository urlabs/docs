export default {
  beats: [
    {
      html: `<span class="eyebrow">第 04 章 · 突破</span>
      <h2>注意力</h2>
      <p class="lead">这就是我们的句子,化作<strong>十个词元</strong>——每个词一个——排成一个环。每个词元都是一串数字——一个<em>向量</em>——捕捉着它那个词的含义。但含义并非一成不变:一个词会随着它结交的同伴而改变。</p>
      <p>拿 <span class="tok">it</span> 这个词来说——单独看,它几乎什么也不意味着。那么,一个词元如何找到其他要紧的词元呢?它会付出<em>注意力</em>:它伸向整个句子,掂量其他每一个词,再把它们的含义折叠进自己之中。</p>`,
    },
    {
      html: `<h3>查询与键</h3>
      <p>每个词元都从自己那串数字里产生出两个小信号:一个<strong>查询</strong>(Query,"我在找什么?")和一个<strong>键</strong>(Key,"我能提供什么?")。</p>
      <p>想象拥挤房间里的每个人都同时举起两张卡片——一张搜索请求(查询)和一张名牌(键)。模型会把每一个请求与每一张名牌相互核对,全在一次扫掠中完成。</p>
      <p>两张卡片都不是手写的。每个词元都通过把自己的向量乘以一对<strong>学来的矩阵</strong>,来构建它的查询和键——查询用 Wq,键用 Wk。训练会不断调校这些矩阵,直到恰当的请求能找到恰当的名牌。</p>`,
    },
    {
      html: `<h3>打分:把一切都比一遍</h3>
      <p>每个词元的查询都会与<em>每一个</em>其他词元的键相比对,而每一对配对都挣得一个原始<strong>分数</strong>——当一个请求和一张名牌对得上时分数高,对不上时分数低。这次匹配就是一次<strong>点积</strong>:把两个向量逐项相乘,再把结果加起来,于是一个表示它们对齐有多强的数字便冒了出来。看 <span class="tok">it</span> 如何一次性把整句话掂量个遍。</p>
      <p class="aside">十个词元意味着 100 次比较。词数翻一倍,工作量就翻两番——这正是为什么很长的文本处理起来代价高昂。</p>
      <p class="aside">真实的文本生成里有一个讲究:每个词元只能给排在它<em>前面</em>的词打分。模型正忙着预测下一个词,所以未来始终被遮住——它不能偷看答案。在这里我们让 <span class="tok">it</span> 在整个环上自由漫游,但在一个真正工作的 LLM 里,朝前的那些光束会保持熄灭。</p>`,
    },
    {
      html: `<h3>Softmax:锐化为权重</h3>
      <p>首先,原始分数会被缩放——除以 <strong>√dₖ</strong>,也就是每个键里数字个数的平方根——这能阻止它们变得过大,以致下一步陷入不稳定。接着,它们穿过 <strong>softmax</strong>,被变成加起来等于 100% 的百分比——也就是注意力<strong>权重</strong>。</p>
      <p>而且 softmax 生来就偏心:它在分配总额之前,会先对每个分数取指数,于是最强的那个匹配会远远占去最大的一份,而弱的匹配则渐渐淡向虚无。此刻,<span class="tok">it</span> 那些散落的分数,塌缩成了一个清晰的焦点。</p>
      <div class="deepdive-row"><a class="deepdive" data-route="/deep/attention-math">注意力方程</a></div>`,
    },
    {
      html: `<h3>汇集这些值</h3>
      <p>每个词元还携带着一个信号:一个<strong>值</strong>(Value)——它真正拿出来提供的内容。如果说键是名牌,那么值就是当你选择倾听时,那个词元<em>告诉你</em>的东西。和查询、键一样,它也来自一个施加在词元向量上的学来矩阵——Wv。</p>
      <p>现在,每个词元都拉入<strong>所有人的值的加权混合</strong>——从它打了高分的词那里取来很多,从其余的那里几乎一无所取。这个混合实实在在就是一次求和:每个值都乘以它的注意力权重,然后全部加在一起。信息沿着明亮的光束<em>流入</em>,而这个词元的含义,被改写成它所发现的一切相关之物的混合。</p>`,
    },
    {
      html: `<h3>"it" → "cat"</h3>
      <p>这就是全部的魔力,浓缩在一幅画里。<span class="tok">it</span> 这个词扫过整句话,锁定了 <span class="tok">cat</span>——模型完全靠自己<strong>弄清楚了 "it" 指代的是什么</strong>。</p>
      <p>语言学家把这叫作<em>共指</em>(coreference),它出了名地难。没有任何规则写下这一点。它是从注意力中自然落出来的——梯度下降只是不断塑造查询矩阵和键矩阵,直到来自 <span class="tok">it</span> 的查询与来自 <span class="tok">cat</span> 的键对齐,因为这种对齐让下一个词更容易预测。</p>`,
    },
    {
      html: `<h3>许多个头同时运作</h3>
      <p>到目前为止,我们只跟踪了一次比较。真实的模型会并排运行<strong>许多注意力头</strong>,每个头都有自己的查询、键和值——于是每个头都能去搜寻一种不同的关系。</p>
      <p>这里有三个:<span style="color:var(--cyan)">句法</span>(语法上的联系)、<span style="color:var(--magenta)">共指</span>(什么指代什么)、<span style="color:var(--amber)">邻接</span>(相邻的词)。一个真实的层会运行几十个。</p>
      <p>这些头并不轮流上阵——它们<strong>并行</strong>运行,各自处理向量中属于自己的那一片。它们的输出会被<strong>拼接</strong>回成一个整体,再穿过最后一个学来的投影,于是下一层看到的是单个混合好的答案。</p>
      <p class="aside">把鼠标悬停在任意词元上,即可探索它注意向何处。</p>`,
    },
    {
      html: `<h3>诀窍就在这里</h3>
      <p>每个词元都在同一时刻运行这一套同样的例程——<strong>查询、键、值;打分、加权、混合</strong>——而每个词元出来时,都在所有其他词元的映照下被重写了一遍。</p>
      <p>这是词元之间真正共享信息的唯一一处——在其他任何地方,网络都是各自单独处理每个词元的。把这个操作一层叠一层地堆起来——注意力混合,然后逐词元精炼——你就得到一个 Transformer:每一个现代语言模型内部的引擎。</p>
      <div class="postcard">注意力让每个词元都能看向其他每一个词元,并取走它所需要的东西。这就是全部的诀窍。</div>
      <div class="deepdive-row">
        <a class="deepdive" data-route="/deep/attention-math">注意力的数学</a>
        <a class="deepdive" data-route="/transformer">下一章:Transformer →</a>
      </div>`,
    },
  ],
}
