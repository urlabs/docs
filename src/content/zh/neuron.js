export default {
  beats: [
    {
      html: `<span class="eyebrow">第 01 章 · 基本构件</span>
      <h2>神经元</h2>
      <p class="lead">人工智能所做的一切,都由同一个微小的部件搭成,重复上百万次。一个神经元——其经典的单一单元形式,即<strong>感知机</strong>(perceptron)——接收一小把数字作为<strong>输入</strong>,把它们浓缩成单个数字。奥妙就在于它<em>如何</em>把它们组合起来。</p>
      <p>想象你在决定要不要去散步。下雨了吗?你有多忙?有朋友约你一起吗?每个事实左右你的程度都不一样——这个"它有多要紧"就是它的<strong>权重</strong>。神经元把每个输入乘以它的权重,再把结果加总——每个输入 <span class="tok">x</span> 对上它的权重 <span class="tok">w</span>,两两配对再相加。数学家把这一步称为<strong>点积</strong>,写作 <span class="tok">w·x</span>。</p>
      <p>最后,它再加上一个<strong>偏置</strong>(bias)&nbsp;<span class="tok">b</span>:一种基准倾向,就像有的人天生就爱说"好"。到此为止的累加是 <span class="tok">w·x + b</span>——许多数字进入,一个累加的总和输出。接下来我们就看看这个总和会触发什么。</p>`,
    },
    {
      side: 'right',
      html: `<h3>这道门</h3>
      <p>这个累加的总和随后会遇到一道门。回到散步那件事:你不会因为每一丝微弱的心动就夺门而出——你的意愿得先积累起来。神经元也是同样的道理。当总和攀过一个<strong>阈值</strong>,它就开始<strong>激发</strong>,把一个更强的信号传递下去。但"激发"只是个比喻——没有什么东西会像电灯开关那样啪地一下翻转。响应是平滑地向上弯曲,是一道陡峭的弯,而非骤然的跳变。</p>
      <p>这道门就是<em>激活</em>,写作 <span class="tok">σ</span>,于是一个神经元完整的输出就是 <span class="tok">y = σ(w·x + b)</span>——加权、求和、偏移,然后弯折。这道弯就是<strong>非线性</strong>,一个听起来很花哨的说法,意思是"响应不是一条直线"。<span class="tok">sigmoid</span> 描出一条柔和的 S 形曲线,把任何数字都挤进 0 到 1 的范围;<span class="tok">tanh</span> 做的是同样的事,只是以零为中心;<span class="tok">ReLU</span> 则干脆就是 <span class="tok">max(0, x)</span>——留下正数,把其余的归零,一道干净利落的折点。</p>
      <p>这道弯就是一切。只堆叠直线式的步骤,整个堆叠又会塌回成一条单独的直线——一百层并不比一层更高明。加上这道弯,这套数学就不再被折平:一层又一层平缓的曲线,能描摹出惊人复杂的模式。只要神经元足够多,一个网络几乎能近似任何函数。</p>
      <p class="aside">看着核心随着总和攀升而变亮,然后在它越过阈值时陡然升起——而非啪地一下跳变。</p>`,
    },
    {
      html: `<h3>一层</h3>
      <p>单个神经元只问一个问题,所以它只能发现一种模式。真实的问题需要许多神经元同时上阵。一<strong>层</strong>就是一排<em>并行</em>工作的神经元——肩并肩,在同一时刻一起运作。每个神经元都读取完全相同的输入,却各自带着一套私有的权重。</p>
      <p>于是每个神经元最终都被调校得对不同的东西敏感——一个盯着天气,另一个盯着你的空闲时间,还有一个盯着谁会一起来。相同的事实进入,提出的问题却各不相同。它们合在一起,能注意到任何单个神经元独自都无法察觉的东西。</p>
      <p>把这些私有的权重行堆叠起来,它们就组成一张网格——一个<strong>矩阵</strong>(matrix)&nbsp;<span class="tok">W</span>——于是整层的工作就缩成了一行,<span class="tok">σ(Wx + b)</span>:一次矩阵乘法、一个偏置、一道弯折,所有神经元一次完成。这和单个神经元做的是同一件事,只是成批运行——这正是为什么这套数学在专为矩阵乘法打造的硬件上能飞速运转。</p>`,
    },
    {
      side: 'right',
      html: `<h3>一个网络</h3>
      <p>现在把层层堆叠起来,每一层都馈入下一层:<span class="tok">input</span> → <span class="tok">hidden</span> → <span class="tok">hidden</span> → <span class="tok">output</span>(输入 → 隐藏 → 隐藏 → 输出)。输入层接收原始数字。中间的<strong>隐藏</strong>层——之所以叫隐藏,是因为外界从不直接读取它们——把这些数字提炼成越来越丰富的模式。输出层报告答案。</p>
      <p>在底层,它不过是简单的嵌套:每一层的输出向量都成为下一层的输入,所以整个网络只是 <span class="tok">σ(Wx + b)</span> 一次又一次地把自己包裹起来——函数彼此复合,一个嵌进另一个之内。</p>
      <p>深度正是关键所在。每一层都把前一层发现的东西,组合成某种更抽象的东西——很像笔画组成字母,字母又组成词。理论上,一个足够宽的层几乎什么都能拟合;而深度只是用低得多的代价抵达同一处,每一层都在前一层的成果之上继续构建。</p>`,
    },
    {
      html: `<h3>前向传播</h3>
      <p>看着它运行才是最精彩的部分。从左侧边缘喂入数字,它们便向右荡漾开去,每一层都唤醒下一层,直到答案在最远端亮起。这一次从左到右的扫掠就是一次<strong>前向传播</strong>——穿过网络的一趟旅程,把一个输入变成一个输出。</p>
      <p>那道行进的波,就是整套计算在按顺序铺展开来:所有那些加权求和与平滑的弯折,一层接一层地亮起——而且顺序是严格的,因为每一层都需要前一层先完成。把权重固定下来,它就是完全可重复的:相同的数字进入,总会给出相同的答案。这,确确实实就是网络在<em>思考</em>——而每当你向我们正一步步建造的那些模型发出提示,运行的正是这同一次扫掠。</p>
      <p class="aside">跟随那道明亮的脉冲,看它从输入越过到输出。</p>`,
    },
    {
      side: 'right',
      html: `<h3>为什么它行得通</h3>
      <p>没有哪个单独的神经元是聪明的——每个只不过权衡几个数字,再把结果弯折一下。智能在于堆叠:一层叠一层,每道门都添上一点点弯曲,直到整个复合体几乎能把自己塑造成你能想象的任何模式。</p>
      <p>每一个权重和偏置都是一个可调的旋钮——一个小网络有成千上万个,一个大网络则有数十亿个。而这一切都不是手写出来的:旋钮一开始只是随机的噪声,网络是被<em>教</em>会正确数值的,一个例子接着一个例子,直到它的答案变得正确。而那场搜索,正是我们接下来要去的地方。</p>
      <p class="aside">这种威力有一个正式的名字——<em>通用近似定理</em>(universal approximation theorem):只要神经元足够多,一个网络就能以你想要的任意精度去逼近任何连续函数。它许诺这样的权重存在,却从不告诉你如何找到它们。</p>
      <div class="postcard">一个神经元就是经过一道门的加权求和。把足够多的神经元堆叠起来,你就得到一个几乎能把自己塑造成任意形状、贴合万物的系统。</div>
      <div class="deepdive-row"><a class="deepdive" data-route="/training">下一章:学习 →</a></div>`,
    },
  ],
}
