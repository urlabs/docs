export default {
  beats: [
    {
      html: `<span class="eyebrow">深入解析 · 嵌入</span>
      <h2>分词</h2>
      <p class="lead">模型既不读词也不读字母——它们读<strong>词元</strong>:子词块(subword)。一套词级的词汇表会需要数百万个条目,却仍然会在错别字、生僻词和新出现的专有名词上栽跟头。逐字符的序列避开了那个陷阱,但会变得太长,从而拖累模型的上下文窗口——它一次能容纳的那段定长输入。子词词元在两者之间取了折中。</p>
      <p>从一个词的原始字符开始——每个字母都是它自己独立的符号。</p>`,
    },
    {
      html: `<h3>合并最频繁的那一对</h3>
      <p><strong>字节对编码(Byte-Pair Encoding,BPE)</strong>会扫描整个训练语料库——数十亿词的文本——数清每一对相邻的符号,再把最常见的那一对融合成一个全新的词元。那个词元进入词汇表,然后从头重新扫描。</p>
      <p class="aside">像 <span class="tok">to</span>、<span class="tok">ke</span> 和 <span class="tok">on</span> 这样的高频对会最先合并:它们出现在成千上万个英语词里,所以每一次合并都能在一步之内削去最多的序列长度。</p>`,
    },
    {
      html: `<h3>继续合并</h3>
      <p>重复成千上万次。常见的字符串塌缩成单个词元;生僻或新奇的字符串则保持被切成更小的碎片。这就是那个关键的洞见:模型可以处理<em>任何</em>词——哪怕是训练中从未见过的——办法就是用熟悉的子词碎片把它拼起来。</p>
      <p class="aside">GPT-2 大约跑了 50,000 次合并;现代的大模型会跑 100,000 次或更多,得到相应规模的词汇表。</p>`,
    },
    {
      html: `<h3>词元与词汇表</h3>
      <p>在所有合并之后,你就有了一张固定的<strong>词汇表</strong>——一张把每个碎片映射到一个整数 ID 的查找表。模型从不去看原始文本;它看到的是一串 ID。"tokenization" 最终只变成两个:<span class="tok">token</span> + <span class="tok">ization</span>。</p>
      <p>一个像 <em>the</em> 这样的常见词挣得一个单独的 ID;一个像 <em>antidisestablishmentarianism</em> 这样的生僻词则被切成许多个。词形上相关的词常常共享词元前缀,这给了模型在学习它们含义时一个先发优势。</p>
      <div class="postcard">分词把原始文本变成一串整数 ID——一个模型真正去读取、预测并生成的原子,一次一个。</div>`,
    },
  ],
}
